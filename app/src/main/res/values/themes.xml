<resources>
    <!-- Base application theme. -->
    <style name="Theme.Gathering" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/peach</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/peach</item>
        <item name="colorSecondaryVariant">@color/peach</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gray</item>
        <!-- Customize your theme here. -->
        <item name="android:timePickerDialogTheme">@style/DialogTheme</item>
        <item name="android:datePickerDialogTheme">@style/DialogTheme</item>

    </style>

    <style name="editTextLayoutStyle">
        <item name="android:layout_margin">20dp</item>
        <item name="counterEnabled">true</item>
        <item name="counterMaxLength">30</item>
    </style>

    <style name="buttonStyle">
        <item name="android:layout_marginLeft">50dp</item>
        <item name="android:layout_marginRight">50dp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/black</item>
    </style>

    //Custom bottom sheet style
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="behavior_peekHeight">800dp</item>
        <item name="behavior_hideable">true</item>
    </style>

    // theme used for datePicker and TimerPicker
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/custom_button_color</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>


</resources>